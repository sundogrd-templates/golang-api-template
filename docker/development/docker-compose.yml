version: '2'
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: sundog-api-dev
    volumes:
     - ../../:/go/src/github.com/sundogrd-templates/golang-api-template
    # environment:
      # - GO111MODULE=on
